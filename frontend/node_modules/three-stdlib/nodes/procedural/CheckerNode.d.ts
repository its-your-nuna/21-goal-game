import { TempNode } from 'three-stdlib/nodes/core/TempNode';
import { FunctionNode } from 'three-stdlib/nodes/core/FunctionNode';
import { UVNode } from 'three-stdlib/nodes/accessors/UVNode';
import { UVTransformNode } from 'three-stdlib/nodes/utils/UVTransformNode';

export class CheckerNode extends TempNode {
    constructor(uv?: UVNode | UVTransformNode);

    uv: UVNode | UVTransformNode;
    nodeType: string;

    copy(source: CheckerNode): this;

    static Nodes: {
        checker: FunctionNode;
    };
}
