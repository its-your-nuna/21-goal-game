import { Node } from 'three-stdlib/nodes/core/Node';
import { NodeBuilder } from 'three-stdlib/nodes/core/NodeBuilder';

export class VarNode extends Node {
    constructor(type: string, value?: any);

    value: any;
    nodeType: string;

    getType(builder: NodeBuilder): string;
    copy(source: VarNode): this;
}
